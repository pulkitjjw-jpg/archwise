# AI Cloud Architecture Generator

> This file was generated by a full manual audit of the codebase (exploration + `npm install`/`typecheck`/`build` + live DB inspection). Every status claim below was independently verified against the actual code, not copy-pasted from a prior summary. See "Known Issues / Debt" for corrections to prior assumptions about this project's state.

## Product Vision

A SaaS tool where a user describes any product idea in plain language. The system:
1. Brainstorms conversationally to understand requirements (multi-turn LLM, not one-shot Q&A).
2. Extracts structured functional + non-functional requirements.
3. Generates architecture in progressive detail: **abstract HLD → multi-cloud (AWS/Azure/GCP) service mapping → LLD (config-level detail) → Terraform export**.
4. As the product grows, generates **incremental delta updates** ("What Changed" diff + cost delta) instead of regenerating from scratch, and supports **manual HLD edits** as an alternative to LLM-driven regeneration.

**Core differentiator**: every architectural decision, at every level of detail, carries a stored, genuine rationale. This is enforced structurally, not just by prompting:
- `HldContent.components[].reasoning` is a required (non-optional) string field in the DB schema — a component literally cannot be persisted without one.
- Cloud mappings carry `alternatives: [{ serviceName, reason }]` — rejected options and why, not just the winner.
- LLD config values carry a parallel `reasoning: Record<string, string>` map, one rationale string per config key.
- The rules engines (`rules-engine.ts`, `lld-rules.ts`, `cloud-mapping.ts`) are deterministic and run first; the LLM's job (`llm.ts`) is to validate/adjust and *narrate* those decisions, not invent architecture from scratch. This is a real architectural boundary, not just a description — see "Reasoning Core" below.

## Tech Stack (verified)

- Next.js 16.2.6 (App Router, Turbopack), React 19.2.6, Tailwind CSS v4
- PostgreSQL + Drizzle ORM 0.45.2 (`drizzle-kit` 0.31.10), `pg` driver
- LLM calls via **OpenRouter**, hardcoded to `google/gemini-2.5-flash`, in `src/lib/llm.ts` — all three functions (`getNextBrainstormTurn`, `extractRequirementsFromHistory`, `validateAndGenerateArchitecture`) call OpenRouter directly via `fetch`, no SDK
- `package.json` name is still the scaffold default `"nextjs-postgresql-template"` — never renamed

### Local environment (verified working as of this audit)
- Postgres 16 running locally via `brew services` (`postgresql@16`), reachable at `127.0.0.1:5432`
- Database `app_db` exists with all 5 tables present and matching `schema.ts` exactly (`projects`, `conversations`, `requirements`, `architectures`, `planning_artifacts`)
- Both Drizzle migrations (`0000_ambiguous_doctor_octopus`, `0001_bent_zarek`) are applied and recorded in `drizzle.__drizzle_migrations` — **no drift** between `schema.ts` and live DB
- `npm install` → clean (408 packages, 6 moderate audit advisories, not investigated further)
- `npm run typecheck` (`tsc --noEmit`) → **clean, zero errors**
- `npm run build` (`next build`) → **clean**, all routes compile (5 API route groups + 2 pages)
- `npm run lint` → **9 errors, 6 warnings** (does not block build — see Known Issues)

## Phase Status (verified against code, not assumed)

### Phase 1 (MVP) — CONFIRMED COMPLETE
- Project creation + chat intake: `POST /api/projects` (`src/app/api/projects/route.ts`), `IntakeForm.tsx`
- Multi-turn brainstorm with stage tracking (`intake` → `brainstorm` → `requirement_gathering`), 6-turn stop condition: `getNextBrainstormTurn` in `llm.ts`, driven through `POST /api/projects/[id]/conversations`
- Structured requirement extraction with `not_specified` sentinel for unspecified fields, versioned `requirements` table (integer `version`, always insert-new-row): `extractRequirementsFromHistory` in `llm.ts`, `POST`/`PUT /api/projects/[id]/requirements`
- Manual requirement editing in `RequirementsPanel.tsx` (edit mode, per-field "+ specify" shortcuts for `not_specified` fields)
- Abstract HLD generation via deterministic `runRulesEngine()` (`src/lib/rules-engine.ts`) — 7 rule groups (CDN, compute serverless-vs-container, relational-vs-document DB, object storage, cache, queue+worker, auth), each component's `reasoning` field is mandatory
- Generation lock when critical fields (`expectedScale`, `budget`, `dataNature`) are `not_specified`, enforced client-side in `ArchitectureWorkspace.tsx` (`isGenerationBlocked`)
- AWS service mapping with alternatives + cost bands: `getCloudMapping()` in `src/lib/cloud-mapping.ts`

### Phase 2 — CONFIRMED COMPLETE
- Azure + GCP mapping extended in the same `cloud-mapping.ts` (single function branches on `provider`), computed for all three providers at generation time in `POST /api/projects/[id]/architectures` (not on-demand)
- Provider toggle UI + side-by-side comparison table with per-cell cost + assumptions: `ArchitectureWorkspace.tsx` "Compare Clouds" view mode
- LLM-generated overall recommendation (`recommendedProvider`, `rationale`, `keyTradeoffs`) cached in `architectures.reasoning.recommendation`
- LLD generation per provider: `runLldRulesEngine()` in `src/lib/lld-rules.ts`, shown in an expandable accordion in the component drawer (`isLldExpanded` state)
- Terraform export: `generateTerraformCode()` in `src/lib/terraform-generator.ts` — generates `main.tf`, `networking.tf`, `compute.tf`, `database.tf`, `storage.tf`, `variables.tf`, `outputs.tf`, `README.md` per provider, with LLD reasoning embedded as `# LLD config [...]` comments; zipped via `jszip` in `GET /api/projects/[id]/export`
- Frontend error banner + retry: confirmed in `ArchitectureWorkspace.tsx` (red banner with Retry/Dismiss on generation failure)
- "Generation Locked" panel with click-to-focus: confirmed — clicking "specify ➜" calls `onSwitchTab("requirements", fieldName)`, which `WorkspaceTabs.tsx` forwards as `focusField` to `RequirementsPanel.tsx`, which auto-enters edit mode, scrolls to and highlights the specific input

### Phase 3 Step 1 (delta-based re-architecture) — CONFIRMED COMPLETE
- Growth trigger reuses the *same* conversation endpoint (`POST /api/projects/[id]/conversations`), not a separate engine — `ChatArea.tsx` detects `isGrowthPhase` from the latest message's stage and tags new user messages `stage: "growth_trigger"`; `llm.ts`'s `getNextBrainstormTurn` has a distinct system prompt branch for `isGrowthPhase`
- New versioned `requirements` + `architectures` rows are created (never mutated), version = integer for requirements, semver-patch-bump string (e.g. `0.1.0` → `0.1.1`) for architectures
- "What Changed" diff panel (added/modified/removed + per-provider cost delta) rendered in `ArchitectureWorkspace.tsx`, sourced from `architectures.reasoning.diff` — populated either by the LLM (auto-generate path, comparing component `id`s) or computed directly in TypeScript (manual-edit path, see below)
- Version dropdown for read-only browsing of historical architecture versions: confirmed (`versionList` state + `<select>`, fetched via `GET /api/projects/[id]/architectures?all=true`)

### Phase 3 Step 2 (interactive manual HLD editor) — **PARTIALLY BUILT, not "zero code" as previously assumed**

This is the most important correction from this audit. `ArchitectureWorkspace.tsx` already contains a substantially working manual editor, and there is a dedicated, functioning backend endpoint for it:

**Already implemented:**
- Edit-mode toggle (`isEditing`/`draftHld` state, deep-cloned from the live architecture so edits are a draft until saved)
- Add component (form: type + name + optional reasoning) and remove component (× button on each SVG node)
- Add connection (from/to/protocol form) and remove connection (× chip in the active-connections list)
- Edit a component's top-level `reasoning` text (textarea in the drawer, edit mode only)
- Edit LLD config values and per-key LLD reasoning strings, per cloud provider, for the currently selected component (drawer accordion, edit mode only)
- **Live client-side validation** while editing: `validateArchitectureLayout()` (`src/lib/validation.ts`) runs reactively on every `draftHld`/`activeProvider` change, surfacing hard errors (orphaned components, compute-without-database) as blockers and soft warnings (CDN/LB wired directly to DB, cost exceeding stated budget) — save is disabled while `errors.length > 0`
- Save flow: `POST /api/projects/[id]/architectures/manual` (`src/app/api/projects/[id]/architectures/manual/route.ts`) — auto-fills cloud mappings/LLD baselines for any newly-added component that lacks them, re-runs `validateArchitectureLayout` server-side as a gate, computes the added/removed/modified diff **in TypeScript** (not via LLM, unlike the auto-generate path) by comparing component IDs and LLD config keys against the previous version, and inserts a new versioned `architectures` row exactly like the auto-generate path does
- Manually-touched components are tagged `metadata: { isManuallyAdded, overrideSource: "user" }` and rendered with a dashed border + amber "U" badge in the SVG diagram to distinguish them from rule-engine output

**Genuinely not implemented (real remaining scope for this step):**
- No way to change which cloud *service* a component is bound to (e.g. swap AWS Lambda → Fargate) — only LLD config values under the already-assigned service are editable
- No drag-to-reposition on the canvas — node position is still fully computed by the fixed column-bucketing layout algorithm (`getLayout()`), so manually added components just fall into a column by `type`
- No cycle detection or connection-count limits beyond the orphan/dependency checks already in `validation.ts`
- No undo/redo within a single edit session (only "Cancel" which discards the whole draft)

**Recommendation before writing new code for this step**: read `ArchitectureWorkspace.tsx` in full and decide whether the remaining scope above is what "Step 2" is meant to cover, or whether the intent was a different UI altogether (e.g., a draggable canvas). Don't re-build what's already there.

## Versioning Pattern (applies throughout)

`projects` (1 row per project, mutable `currentVersion` pointer) →
`conversations` (append-only, tagged with `stage`, never updated) →
`requirements` (append-only, integer `version`, `PUT`/extraction always inserts a new row — never updates in place) →
`architectures` (append-only, semver-string `version` with the patch segment bumped on every generate/manual-save, each row is a **full self-contained snapshot**: complete `hld` (components + connections + all three providers' cloud mappings + LLD) and `reasoning` (decisions, assumptions, risks, recommendation, diff) — nothing is normalized out into separate decision/mapping tables, despite the original planning scaffold (`planning-data.ts`) describing a more normalized schema)

Both `architectures` insert paths (LLM auto-generate in `architectures/route.ts`, and manual edit in `architectures/manual/route.ts`) independently compute a `diff` block against the immediately-previous version by `createdAt desc`, and both bump `projects.currentVersion` after insert.

## Key File Locations

| Subsystem | File |
|---|---|
| DB schema (all tables + JSON column types) | `src/db/schema.ts` |
| DB client | `src/db/index.ts` |
| Rules engine (abstract HLD) | `src/lib/rules-engine.ts` |
| Cloud service mapping (AWS/Azure/GCP + cost bands) | `src/lib/cloud-mapping.ts` |
| LLD rules engine (per-provider config) | `src/lib/lld-rules.ts` |
| Terraform generator | `src/lib/terraform-generator.ts` |
| Manual-edit layout validation | `src/lib/validation.ts` |
| LLM layer (all OpenRouter calls) | `src/lib/llm.ts` |
| Project creation | `src/app/api/projects/route.ts` |
| Conversation turns (brainstorm + growth trigger) | `src/app/api/projects/[id]/conversations/route.ts` |
| Requirements (extract/edit) | `src/app/api/projects/[id]/requirements/route.ts` |
| Architecture generate (LLM-driven) | `src/app/api/projects/[id]/architectures/route.ts` |
| Architecture manual save | `src/app/api/projects/[id]/architectures/manual/route.ts` |
| Terraform export (zip) | `src/app/api/projects/[id]/export/route.ts` |
| Project workspace page | `src/app/projects/[id]/page.tsx` |
| Chat UI | `src/app/components/ChatArea.tsx` |
| Requirements UI | `src/app/components/RequirementsPanel.tsx` |
| Architecture workspace (diagram, compare, manual editor, terraform export) | `src/app/components/ArchitectureWorkspace.tsx` |
| Tab container wiring requirements ↔ architecture focus-shortcuts | `src/app/components/WorkspaceTabs.tsx` |

## Known Issues / Debt (verified during this audit)

1. **No git repository.** This directory has no version control at all — no history, no rollback, nothing to diff against. Given three phases of work already exist, this is real risk. Recommend `git init` + initial commit before further changes.
2. **Homepage (`src/app/page.tsx`) is leftover scaffold content, not a real landing/dashboard page.** It renders a static "planning artifacts" showcase (`productContext`, `planningArtifacts` seeded from `src/lib/planning-data.ts` into the `planning_artifacts` table, served via `getPlanningArtifacts()` / `GET /api/planning/phase-1`) describing the *original Phase 1 plan* — copy like "Recommended next move: Build the Phase 1 technical specification first" is now stale since Phases 1–2 are complete. The real `IntakeForm` is embedded in this page so project creation still works, but there is **no project list/dashboard view** — once redirected to `/projects/[id]` after creation, there's no in-app way to navigate back to a previously created project without knowing its UUID.
3. **The `planning_artifacts` table / `planning-data.ts` / `planning-store.ts` / `seed.ts` / `/api/planning/phase-1` are a vestigial meta-layer**, not part of the product's actual domain model (`projects`/`conversations`/`requirements`/`architectures`). They describe the build plan for a human/Claude Code audience, seeded once and displayed on the homepage. Worth deciding whether to keep (as a living plan doc), repurpose (as a real project list), or delete now that the plan they describe is largely executed.
4. **`.env` contains a live-looking OpenRouter key**, while `.env.example` already carries a warning: `"WARNING: Replace with your own OpenRouter key before deployment. Do not reuse credentials from other workspaces."` This confirms the concern was already flagged once before — worth explicitly confirming with the user whether this key is now isolated to this project or still shared, especially since the LLM provider setup may change now that Claude Code is driving development.
5. **`llm.ts` hardcodes `"HTTP-Referer": "https://github.com/google-deepmind/antigravity"`** in all three OpenRouter request headers (`getNextBrainstormTurn`, `extractRequirementsFromHistory`, `validateAndGenerateArchitecture`). This is an artifact copy-pasted from a different tool's (Google's Antigravity IDE) boilerplate — harmless functionally (OpenRouter just uses it for referrer analytics) but should be updated to reflect this actual project.
6. **`package.json`'s `"name"` field is still `"nextjs-postgresql-template"`** — never renamed from the scaffold.
7. **`npm run lint` reports 9 errors and 6 warnings** (does not block `next build`, which passed clean). The errors are concentrated in `react-hooks/set-state-in-effect` (calling a state-setting async function directly in a bare `useEffect`, in `ChatArea`-adjacent components: `RequirementsPanel.tsx`, `WorkspaceTabs.tsx`, `ArchitectureWorkspace.tsx`) plus two "variable used before its declaration" ordering issues (`startEditing` in `RequirementsPanel.tsx`, `calculateTotalCost` in `ArchitectureWorkspace.tsx`) and one unescaped apostrophe (`react/no-unescaped-entities`, line 1462 of `ArchitectureWorkspace.tsx`). Not urgent, but real lint debt under React 19 / Next 16's stricter default rules.
8. **No `walkthrough.md` or similar doc trail from the previous IDE session was found anywhere in the repo** — confirmed clean, nothing to reconcile.

## Go / No-Go for Phase 3 Step 2

Given finding #Phase-3-Step-2 above, **do not start from scratch**. The manual HLD editor already has a working add/remove/edit/validate/save loop wired end-to-end through a real API endpoint. Before writing code, decide with the user which of the "genuinely not implemented" items above (service-swap, drag-to-reposition, cycle detection, undo) are actually in scope for "Step 2," since the original task description ("implementation was interrupted before any code was written") does not match what's in the repo.
